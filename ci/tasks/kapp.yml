platform: linux

image_resource:
  type: docker-image
  source: {repository: mwrightregistry.azurecr.io/tools, tag: latest}

inputs:
  - name: venues

params:
  KUBECONFIG: 
  CONTEXT: 

run:
  path: "venues/ci/tasks/kapp.sh"